[package]
name = "asap-control-lower"
version = "0.1.0"
edition = "2021"

[dependencies]
asap-control-core = { path = "../core" }
# Private mirror of GreptimeTeam/promql-parser (Apache-2.0), vendored so we can
# carry local PromQL grammar/function additions ahead of upstream releases.
promql-parser = { git = "https://github.com/ProjectASAP/promql-parser", branch = "main" }
