[package]
name = "asap-control-lower"
version = "0.1.0"
edition = "2021"

[dependencies]
asap-control-core = { path = "../core" }
datafusion = "43"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros", "rt-multi-thread"] }
